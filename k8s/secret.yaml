apiVersion: v1
kind: Secret
metadata:
  name: workhub-secrets
  namespace: workhub-prod
type: Opaque
stringData:
  # Base64 encoded values would be used in a real pipeline or sealed secrets
  DB_USERNAME: "postgres"
  DB_PASSWORD: "securepassword"
  JWT_SECRET: "your-very-long-and-secure-jwt-secret-key-for-prod"
  RABBITMQ_PASSWORD: "guest"
